export const enum NEWLY_REGISTER_USER {
  message = 'has been registered',
}
export const enum REPORTED_USER {
  message = 'reported',
}
export const enum DELETED_USER {
  message = 'deleted his/her account',
}
